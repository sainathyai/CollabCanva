# CollabCanvas Environment Variables Example
# Copy this file to frontend/.env and backend/.env and fill in your actual values

# ============================================
# FRONTEND ENVIRONMENT VARIABLES
# ============================================
# Copy these to frontend/.env

# WebSocket URL (point to your backend server)
VITE_WS_URL=ws://localhost:8080

# Firebase Authentication Configuration
VITE_FIREBASE_API_KEY=your_firebase_api_key
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# ============================================
# BACKEND ENVIRONMENT VARIABLES
# ============================================
# Copy these to backend/.env

# Server Configuration
PORT=8080

# CORS: Comma-separated list of allowed frontend origins
ALLOWED_ORIGINS=http://localhost:5173

# Firebase Project ID (for token verification)
FIREBASE_PROJECT_ID=your_project_id

# ============================================
# DEPLOYMENT NOTES
# ============================================
# 
# For staging/production:
# - Frontend VITE_WS_URL: Use wss:// protocol and deployed backend URL
# - Backend ALLOWED_ORIGINS: Add your deployed frontend URL (e.g., https://your-app.vercel.app)
# - Firebase: Ensure your staging/production domains are added to Firebase authorized domains
#
