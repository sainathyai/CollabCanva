# Backend Environment Variables

# Server Configuration
PORT=8080
NODE_ENV=development

# CORS - Comma-separated list of allowed origins
# For development: http://localhost:5173
# For production: https://your-frontend-domain.vercel.app
ALLOWED_ORIGINS=http://localhost:5173

# Firebase Configuration (Optional)
# Leave empty for development mode (mock authentication)
# For production, set this to enable real Firebase auth
FIREBASE_PROJECT_ID=

# ====================
# AWS Configuration
# ====================

# AWS Region (default: us-east-1)
AWS_REGION=us-east-1

# AWS Credentials
# Get these from AWS IAM Console or use AWS CLI configured credentials
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# DynamoDB Table Names
DYNAMODB_PROJECTS_TABLE=collabcanvas-projects
DYNAMODB_OBJECTS_TABLE=collabcanvas-objects
DYNAMODB_COLLABORATORS_TABLE=collabcanvas-collaborators
DYNAMODB_USERS_TABLE=collabcanvas-users

# ====================
# DEPLOYMENT NOTES
# ====================

# For Render/Fly.io/Railway:
# - Set NODE_ENV=production
# - Set ALLOWED_ORIGINS to your frontend URL
# - Optionally set FIREBASE_PROJECT_ID for production auth
# - The PORT will be provided by the platform

# For Docker:
# - Copy this file to .env and fill in the values
# - Run: docker build -t collabcanva-backend .
# - Run: docker run -p 8080:8080 --env-file .env collabcanva-backend


